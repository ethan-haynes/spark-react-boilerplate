# tricks.yml
# optional for those who want to install watchdog globally
tricks:
  - watchdog.tricks.ShellCommandTrick:
      patterns: ["*.java", "*/index.js", "*.jsx"]
      shell_command: "npm run killServer && webpack && mvn compile && mvn exec:java"
