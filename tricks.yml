# tricks.yml
# optional for those who watch to install watchdog globally
tricks:
- watchdog.tricks.ShellCommandTrick:
    patterns: ["*.java", "*/index.js", "*.jsx"]
    shell_command: "webpack && mvn compile && mvn exec:java"
